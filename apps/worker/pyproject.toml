[project]
name = "contractlens-worker"
version = "0.1.0"
description = "ContractLens ingest worker - PDF/URL/text processing"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
